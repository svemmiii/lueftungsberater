Lüftungsberater v0.6.24 – Pytest Fix v4

Änderung:
- tests/test_config_flow.py:
  test_room_schema_accepts_required_room_inputs_with_filtered_selectors
  sendet jetzt SECTION_ROOM_NIGHT: {} mit.

Warum:
_room_schema() definiert SECTION_ROOM_NIGHT als vol.Required(...).
Der bisherige Test ließ diese erforderliche Section vollständig weg und
scheiterte deshalb bei der Schema-Validierung, bevor die eigentlichen
Sensor-Assertions erreicht wurden.

Keine Produktionslogik wurde verändert.

Separat:
Die GitHub-Warnung wegen /manifest.json verschwindet erst, wenn die
versehentliche manifest.json im Repository-Root dauerhaft gelöscht wird.
custom_components/lueftungsberater/manifest.json muss natürlich bleiben.
