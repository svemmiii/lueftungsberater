Lüftungsassistent v0.7.1 – Patch from v0.7.0

Upload the contents of this ZIP at the repository root while preserving folders.
It contains only files that changed from the previously released/uploaded v0.7.0 tree, plus v0.7.1 release/audit notes.

Main fixes:
- duplicate notification choices
- warning/all-clear notifications once per assistant
- configurable night end honored everywhere + end timer
- remote display_mode restored
- Remote/Tailscale parent-picker migration actually runs (MINOR_VERSION 7)
- CI no longer hides bad root layout
- localized_texts removed from room state; per-user card localization via WebSocket
- white Safety-Lock only on the colored detailed-card header
- active remote-query icon on detailed room cards
- transient remote/warning attributes omitted when inactive
